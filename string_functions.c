#include <stdio.h>
#include <string.h>
int main()
{
    char string1[] = "BOB", string2[] = "Bro";
    // strlwr(string1);
    // strupr(string1);
    // strcat(string1, string2);
    // strncat(string2, string1, 2);
    // strcpy(string1, string2);
    // strncpy(string1, string2, 2);

    // strset(string1, '?');
    // strnset(string1, 'x', 2);
    // strrev(string2);

    // int res = strlen(string1);
    // int res = strcmp(string1, string2);
    // int res = strncmp(string1, string2, 1);
    // int res =  strcmpi(string1, string2);
    // int res =  strnicmp(string1, string2, 1);
    /*if (res == 0)
    {
        printf("\nBoth the strings are same");
    }
    else
    {
        printf("\nBoth the strings are not same");
    }*/
    // printf("%d", res);
    return 0;
}